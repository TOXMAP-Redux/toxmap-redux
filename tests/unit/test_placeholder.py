"""Placeholder unit test — passes until Phase 1 unit tests are added."""


def test_placeholder() -> None:
    """This test exists to ensure pytest finds at least one test in Phase 0."""
    assert True
